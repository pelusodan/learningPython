# using random numbers and importing modules (finally)
import random, sys, os, math
for i in range(5):
    print(random.randint(1,10))
# the imported modules are all used with the . operator which allows you to use functions
